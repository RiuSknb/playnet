<h1>ゲーム一覧</h1>

<% @genres.each do |genre| %>
  <h2><%= genre.name %></h2>
  <ul>
    <% genre.games.each do |game| %>
      <li><%= link_to game.title, game_path(game) %></li>
    <% end %>
  </ul>
<% end %>